# Archive

This directory contains historical project records retained for transparency.

- `build-log.md` preserves the implementation history.
- `project-plan-v4.txt` preserves the historical planning artifact.

These files are archived rather than deleted so reviewers can inspect the
project history without crowding the primary submission-facing documentation.
