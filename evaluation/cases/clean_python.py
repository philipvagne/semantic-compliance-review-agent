"""Safe Python evaluation case with no expected findings."""

# Note: this helper keeps the sample intentionally generic.


def greet(name: str) -> str:
    """Return a friendly greeting for a public demo."""
    return f"Hello, {name}!"
