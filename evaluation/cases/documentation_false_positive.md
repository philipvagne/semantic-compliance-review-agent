# Credential Setup Guide

Set `GOOGLE_API_KEY` or `GEMINI_API_KEY` in your local shell before running
the CLI.

The public API endpoint is documented here for normal developer setup:
`https://api.example.com/v1/review`.
