// Friendly sample comment for JavaScript-family extractor testing.
// TODO: replace the placeholder release note before shipping.

/*
  Sample block comment for extractor coverage.
  NOTE: this example stays artificial and safe.
*/

/**
 * Describe the sample helper for deterministic review testing.
 */
function greet(name) {
  return `Hello, ${name}!`;
}

// FIXME: swap the hard-coded demo label later.
console.log(greet("world"));
